# MovieDB - Spring GraphQL Tutorial

This repository contains the companion code for the [Spring GraphQL Tutorial](https://graphqlguy.com/docs/category/spring-graphql-tutorial) masterclass (13 parts).

Code will be merged to `main` once the course is completed. In the meantime, each class has its own branch (e.g., `class_1`, `class_2`, etc.).